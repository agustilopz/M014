<!DOCTYPE html>
<html lang="ca">
<head>
  <meta charset="UTF-8">
  <title>Gestió de Mangas</title>
  <link rel="stylesheet" href="/nou_css.css">
</head>
<body>
<header>
  <h1>📚 Gestió de Mangas</h1>
  <p>Usuari: <%= user.username %></p>
  <p><%- htmlMessage %></p>  <!-- <- nota el - per no escapar -->
  <a href="/mangas/new">➕ Nou manga</a>
</header>

  <table border="1" style="margin-top: 20px;">
    <tr><th>Títol</th><th>Autor</th><th>Any</th><th>Gènere</th><th>Volums</th><th>Accions</th></tr>
    <% data.mangas.forEach(manga => { %>
      <tr>
        <td><%= manga.title %></td>
        <td><%= manga.author %></td>
        <td><%= manga.year %></td>
        <td><%= manga.genre %></td>
        <td><%= manga.volumes %></td>
        <td>
          <a href="/mangas/edit/<%= manga.id %>">✏️ Editar</a>
          <form action="/mangas/delete/<%= manga.id %>" method="POST" style="display:inline;">
            <button type="submit">🗑️ Eliminar</button>
          </form>
        </td>
      </tr>
    <% }) %>
  </table>
</body>
</html>
